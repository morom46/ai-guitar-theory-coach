/**
 * DEPRECATED — Tone Match merged into Songs & Tones (SongPractice.jsx).
 * Tones now live on each song in src/data/songs.js (localStorage "songs.v2").
 * This file is kept only because the sync layer won't allow deletion;
 * nothing imports it, so it is not bundled. Safe to delete manually.
 */
export default function TonesPage() {
  return null;
}
